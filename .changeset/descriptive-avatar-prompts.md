---
"@charivo/realtime-avatar": patch
"@charivo/realtime": patch
---

Rewrite realtime and avatar prompts in a descriptive tone instead of count-prescriptive language. The avatar instruction addendum now emits expression, motion, and pairing guidance only when those tools are available in the active catalog. Tool descriptions and the default realtime prompt drop quantifiers like "single", "one", and "at most one" in favor of quality framing such as "don't stack body motions in the same reply".
